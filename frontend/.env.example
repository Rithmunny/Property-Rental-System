# Base URL for the backend API (used when VITE_USE_MOCK=false).
# Local: http://localhost:5000. On Vercel, leave unset so the app calls same-origin /api.
VITE_API_URL=http://localhost:5000

# false = call the Express API; true = localStorage mock adapters
VITE_USE_MOCK=false
