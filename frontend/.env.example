# Base URL for the backend API (used when VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:5000

# Use localStorage mock adapters when true (default for frontend-only development)
VITE_USE_MOCK=true
